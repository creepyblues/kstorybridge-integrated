{
  "name": "kstorybridge-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:dashboard": "npm run dev --workspace=apps/dashboard",
    "dev:website": "npm run dev --workspace=apps/website",
    "dev:admin": "npm run dev --workspace=apps/admin",
    "build:dashboard": "npm run build --workspace=apps/dashboard",
    "build:website": "npm run build --workspace=apps/website",
    "build:admin": "npm run build --workspace=apps/admin",
    "build:packages": "npm run build --workspace=packages/ui && npm run build --workspace=packages/api-client && npm run build --workspace=packages/utils && npm run build --workspace=packages/build-config && npm run build --workspace=packages/testing",
    "build:all": "npm run build:packages && npm run build:dashboard && npm run build:website && npm run build:admin",
    "lint:all": "npm run lint --workspace=apps/dashboard && npm run lint --workspace=apps/website && npm run lint --workspace=apps/admin",
    "test:all": "npm run test --workspace=apps/dashboard && npm run test --workspace=apps/website && npm run test --workspace=apps/admin",
    "test:watch": "npm run test:watch --workspace=apps/dashboard",
    "test:coverage": "npm run test:coverage --workspace=apps/dashboard && npm run test:coverage --workspace=apps/website",
    "clean:all": "npm run clean --workspace=apps/dashboard && npm run clean --workspace=apps/website && npm run clean --workspace=apps/admin && npm run clean --workspace=packages/ui && npm run clean --workspace=packages/build-config",
    "storybook": "npm run storybook --workspace=packages/storybook",
    "build-storybook": "npm run build-storybook --workspace=packages/storybook",
    "install:all": "npm install"
  },
  "devDependencies": {
    "@types/node": "^22.5.5",
    "typescript": "^5.5.3"
  },
  "dependencies": {
    "@radix-ui/react-slot": "^1.2.3",
    "@supabase/supabase-js": "^2.53.0",
    "class-variance-authority": "^0.7.1",
    "uuid": "^11.1.0"
  }
}
