{
  "name": "kstorybridge-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:dashboard": "npm run dev --workspace=apps/dashboard",
    "dev:website": "npm run dev --workspace=apps/website",
    "dev:creator": "npm run dev --workspace=apps/creator",
    "build:dashboard": "npm run build --workspace=apps/dashboard",
    "build:website": "npm run build --workspace=apps/website",
    "build:creator": "npm run build --workspace=apps/creator",
    "build:packages": "npm run build --workspace=packages/auth && npm run build --workspace=packages/ui && npm run build --workspace=packages/api-client && npm run build --workspace=packages/utils && npm run build --workspace=packages/build-config && npm run build --workspace=packages/testing",
    "build:all": "npm run build:packages && npm run build:dashboard && npm run build:website && npm run build:creator",
    "lint:all": "npm run lint --workspace=apps/dashboard && npm run lint --workspace=apps/website && npm run lint --workspace=apps/creator",
    "test:all": "npm run test --workspace=apps/dashboard -- --run && npm run test --workspace=apps/website -- --run",
    "test:watch": "npm run test:watch --workspace=apps/dashboard",
    "test:coverage": "npm run test:coverage --workspace=apps/dashboard && npm run test:coverage --workspace=apps/website",
    "clean:all": "npm run clean --workspace=apps/dashboard && npm run clean --workspace=apps/website && npm run clean --workspace=packages/ui && npm run clean --workspace=packages/build-config",
    "storybook": "npm run storybook --workspace=packages/storybook",
    "build-storybook": "npm run build-storybook --workspace=packages/storybook",
    "install:all": "npm install"
  },
  "devDependencies": {
    "@types/node": "^22.5.5",
    "typescript": "^5.5.3"
  },
  "dependencies": {
    "@radix-ui/react-slot": "^1.2.3",
    "@supabase/supabase-js": "^2.53.0",
    "class-variance-authority": "^0.7.1",
    "dotenv": "^17.2.1",
    "openai": "^5.16.0",
    "uuid": "^11.1.0",
    "vercel": "^46.1.1"
  }
}
